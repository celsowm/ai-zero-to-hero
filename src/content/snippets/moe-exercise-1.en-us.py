def get_top_expert(scores):
    # scores: lista de floats (ex: [0.1, 0.8, 0.05, 0.05])
    # retorne o índice do maior score
    return 

# Teste
scores = [0.12, 0.05, 0.73, 0.1]
print(f"Especialista escolhido: {get_top_expert(scores)}")