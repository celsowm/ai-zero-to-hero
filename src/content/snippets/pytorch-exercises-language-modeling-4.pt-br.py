import torch

# Probabilidades (soma 1)
probs = torch.tensor([0.0, 1.0, 0.0])

# TODO: Sorteie UM índice em 'sampled_idx' usando torch.multinomial
# Use num_samples=1 e salve apenas o número (.item())
sampled_idx = None
